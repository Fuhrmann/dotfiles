#!/usr/bin/sh

# Open a new kitty session or focus the current opened window
if pgrep -x "ncspot" > /dev/null
then
    i3-msg "[class=music] focus"
else
    kitty --class music --session $HOME/projetos/dotfiles/dotfiles/config/kitty/ncspot-session.kitty
fi
