# focused border
set_from_resource $fb i3wm.color1 #000

# unfocused border
set_from_resource $ub i3wm.background #000

# unfocused text color
set_from_resource $utc i3wm.color1 #000

# Text color
set_from_resource $tc i3wm.color7 #FFF

# Foreground
set_from_resource $fg i3wm.foreground #f0f0f0

# Background
set_from_resource $bg i3wm.background #f0f0f0

# class                 border  backgr. text indicator child_border
client.focused          $fb     $fb     $tc  $fb       $fb
client.focused_inactive $bg     $bg     $fg  $bg       $bg
client.unfocused        $ub     $ub     $utc $bg       $ub
client.urgent           $bg     $bg     $fg  $bg       $bg
client.placeholder      $bg     $bg     $fg  $bg       $bg
client.background       $ub

# Seta a mod key
set $mod Mod4

# Seta o wallpaper
exec --no-startup-id nitrogen --restore

# Seta o modifier para as janelas float
floating_modifier $mod

# Defining the font
font pango:JetBrains Mono 10

# Adjust volumes
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl -- set-sink-volume @DEFAULT_SINK@ +10%
bindsym XF86AudioLowerVolume exec --no-startup-id pactl -- set-sink-volume @DEFAULT_SINK@ -10%
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle

# Media player controls
bindsym XF86AudioPlay exec playerctl -i chrome play
bindsym XF86AudioPause exec playerctl -i chrome pause
bindsym XF86AudioNext exec playerctl -i chrome next
bindsym XF86AudioPrev exec playerctl -i chrome previous

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+KP_Enter exec i3-sensible-terminal

# Kill focused window
bindsym $mod+w kill

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split orientations
bindsym $mod+h split h
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+shift+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+t layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define workspaces
set $ws1 "1: term"
set $ws2 "2: browse"
set $ws3 "3: code"
set $ws4 "4: game"
set $ws5 "5: chat"
set $ws6 "6: music"
set $ws7 "7: warez"

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Tab workspace back_and_forth

# reload the configuration file
bindsym $mod+Shift+c reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# exit i3 (logs you out of your X session)
set $mode_power  power
bindsym Control+Mod1+Delete mode "$mode_power"
mode "$mode_power" {
    bindsym l mode "default", exec --no-startup-id $HOME/.config/i3/scripts/i3exit lock
    bindsym e mode "default", exec --no-startup-id $HOME/.config/i3/scripts/i3exit logout
    bindsym s mode "default", exec --no-startup-id $HOME/.config/i3/scripts/i3exit suspend
    bindsym h mode "default", exec --no-startup-id $HOME/.config/i3/scripts/i3exit hibernate
    bindsym r mode "default", exec --no-startup-id $HOME/.config/i3/scripts/i3exit reboot
    bindsym p mode "default", exec --no-startup-id $HOME/.config/i3/scripts/i3exit shutdown

    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# resize window (you can also use the mouse for that)
bindsym $mod+KP_Add resize grow width 10 px or 10 ppt
bindsym $mod+KP_Subtract resize shrink width 10 px or 10 ppt

set $mode_resize  resize
mode "$mode_resize" {
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "$mode_resize"

popup_during_fullscreen smart
default_floating_border pixel 2

# Gaps
for_window [class=".*"] border pixel 5
gaps inner 8
smart_gaps on
smart_borders no_gaps

# Open rofi
bindsym Control+space exec --no-startup-id rofi -show

# Take a screenshot
bindsym Control+Print exec --no-startup-id flameshot gui

# Navegar entre workspaces
bindsym Control+Mod1+Right workspace next
bindsym Control+Mod1+Left workspace prev

# Configuration for windows
set $float floating enable, border pixel 2
set $center move position center

for_window [window_role="GtkFileChooserDialog"] $float, resize set 900 600, $center
for_window [class="Arandr"] $float, $center
for_window [title="Exit DBeaver"] $float, $center
for_window [window_role="bubble"] $float, $center
for_window [window_role="task_dialog"] $float, $center
for_window [window_role="Preferences"] $float, $center
for_window [window_type="dialog"] $float, $center
for_window [window_type="menu"] $float, $center
for_window [class="XCalc"] $float, $center
for_window [class="jetbrains-phpstorm" title="win0"] $float, $center
for_window [class="(?i)pavucontrol"] $float, resize set 758 576, $center

for_window [title="Android Emulator"] floating enable
for_window [class="mpv"] border pixel 0
for_window [urgent="latest"] focus
for_window [class="^Steam$" title="Steam - News"] floating enable
for_window [class="^Steam$" title="^Settings$"] floating enable
for_window [class="^Steam$" title=".* - event started"] floating enable
for_window [class="^Steam$" title=".* CD key"] floating enable
for_window [class="^Steam$" title="^Steam - Self Updater$"] floating enable
for_window [class="^Steam$" title="^Screenshot Uploader$"] floating enable
for_window [class="^Steam$" title="^Steam Guard - Computer Authorization Required$"] floating enable
for_window [title="^Steam Keyboard$"] floating enable
for_window [class="^deadcells$"] floating disable, fullscreen enable, workspace --no-auto-back-and-forth $ws3
for_window [title="^music$"] move to workspace $ws6

# Assign windows to workspaces
assign [class="Google-chrome" window_role="browser"] → $ws2
assign [class="jetbrains-phpstorm"] → $ws3
assign [class="^Steam$" title="^Steam$"] → $ws4
assign [instance="web.whatsapp.com" window_role="pop-up"] → $ws5
assign [class="(?i)mpv"] → $ws6
assign [title="^music$"] → $ws6
assign [title="(?i)transmission"] → $ws7

# Open up the most used configurations
bindsym $mod+i exec --no-startup-id subl ~/.config/i3/config
bindsym $mod+p exec --no-startup-id subl ~/.config/polybar/config.ini

# Open up whats app
bindsym $mod+c exec --no-startup-id $HOME/.config/i3/scripts/open_wa.sh

# Move focused window between monitors
bindsym $mod+Shift+greater move workspace to output right
bindsym $mod+Shift+less move workspace to output left

# Move workspaces para monitores
workspace $ws1 output HDMI-0
workspace $ws2 output HDMI-0
workspace $ws3 output HDMI-0
workspace $ws4 output HDMI-0
workspace $ws5 output DP-5
workspace $ws6 output DP-5
workspace $ws7 output DP-5

# Launch polybar
exec_always --no-startup-id $HOME/.config/polybar/launch.sh

# Launch music player
bindsym $mod+m exec --no-startup-id $HOME/.config/i3/scripts/open_music.sh

# Launch file manger
bindsym $mod+f exec --no-startup-id kitty --session $HOME/.config/kitty/filemanager.kitty

# Persist multi monitor layout
exec --no-startup-id autorandr --change --force

# Launch mpris-scrobbler
exec --no-startup-id mpris-scrobbler &